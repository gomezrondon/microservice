spring:
  application:
    name: db-service
  cloud:
    config:
      uri: http://config:8980

management:
  security:
    enabled: true

---
spring:
  profiles: sandbox
  application:
    name: db-service-sandbox
  cloud:
    config:
      uri: http://localhost:8980

management:
  security:
    enabled: true