server:
  port: ${PORT:8761}
spring:
  application:
    name: eureka-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

---

server:
  port: ${PORT:8761}
spring:
  profiles: sandbox
  application:
    name: eureka-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
