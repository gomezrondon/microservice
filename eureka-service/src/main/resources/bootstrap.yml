server:
  port: ${port:0}
spring:
  application:
    name: stock-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
service:
  db-service:
    name: db-reactive-stock

---
server:
  port: ${port:0}
spring:
  profiles:
    active: sandbox
  application:
    name: stock-service-sandbox
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
service:
  db-service:
    name: db-reactive-stock-sandbox