server:
  port: 8099
spring:
  application:
    name: micro-proxy
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server


---

server:
  port: 8099
spring:
  application:
    name: micro-proxy-sandbox
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server