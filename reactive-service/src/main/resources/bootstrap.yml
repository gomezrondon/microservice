server:
  port: ${port:6001}
spring:
  application:
    name: ms-db-rx
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server

---
server:
  port: ${port:6001}
spring:
  profiles:
    active: sandbox
  application:
    name: ms-db-rx-sandbox
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server