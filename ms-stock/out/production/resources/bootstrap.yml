server:
  port: ${port:5001}
spring:
  application:
    name: ms-stock
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server

---
server:
  port: ${port:5001}
spring:
  profiles:
    active: sandbox
  application:
    name: ms-stock-sandbox
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server